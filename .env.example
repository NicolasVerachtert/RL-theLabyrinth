# Environment configuration for RL Project

# Output directory for storing logs, models, and videos
OUTPUT_DIR=./output

# Subdirectories for organizing output files
MODELS_DIR=${OUTPUT_DIR}/models
VIDS_DIR=${OUTPUT_DIR}/vids
TENSORBOARD_DIR=${OUTPUT_DIR}/tensorboard